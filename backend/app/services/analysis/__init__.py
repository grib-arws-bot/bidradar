"""S8 심층분석(설계안 07절, CLAUDE.md). 착수 단계 — 2026-09-01.

지금 들어 있는 것: extract.py(문서 텍스트 추출 폴백 사슬 — HWPX·PDF·HTML은 실제 동작,
HWP·OCR은 인프라 작업 대기 중인 명시적 미구현), match.py(요구사양 충족 판정 규칙 엔진).

아직 없는 것: LLM 추출 호출(A2·A5·A6, ANTHROPIC_API_KEY 필요), 분석 오케스트레이션과
API/화면. CLAUDE.md 3원칙(LLM은 판정 안 함·판정은 권고·자동 실행 금지)은 이미 이 두
모듈의 설계에 반영돼 있다 — match.py는 규칙만으로 판정하고 근거(cite) 없이는 결과를
안 내보낸다.
"""
